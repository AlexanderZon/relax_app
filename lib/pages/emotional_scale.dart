import 'package:flutter/material.dart';

class EmotionalScale extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(
        child: Text('Escala Emocional'),
      ),
    );
  }
}